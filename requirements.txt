# Dashboard runtime only -- this is what Streamlit Community Cloud installs.
# Kept deliberately slim so the hosted app builds fast; the full pipeline
# stack (Airflow, PySpark, dbt) lives in requirements-pipeline.txt and is
# not needed to serve the dashboard, which reads a committed data snapshot.
# upper bounds cap at the next untested major version -- pandas 3.0 already
# broke this project once (a read_json signature change), so floor-only
# pins are a real risk on a repo that won't be watched closely
streamlit>=1.40,<2
plotly>=5.22,<7
pandas>=2.2,<4
pyarrow>=16,<25
python-dotenv>=1.0,<2
boto3>=1.34,<2
